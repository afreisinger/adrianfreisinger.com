import React from "react";
import { PiGraduationCapBold } from "react-icons/pi";

const IconEducation = ({
  size = 24,
  color = "currentColor",
  className = "",
}) => (
  <PiGraduationCapBold
    size={size}
    color={color}
    className={className}
  />
);

export default IconEducation;
